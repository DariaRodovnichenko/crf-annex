import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dariarodovnichenko.annexcrf',
  appName: 'annex-crf',
  webDir: 'dist/annex-crf/browser',
};

export default config;
